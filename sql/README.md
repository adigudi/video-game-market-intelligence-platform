# sql/

Purpose
- Database schema definitions, DDL, and ETL SQL scripts.
- Analytical SQL queries and views used for reporting and extracts.

Suggested contents
- schema.sql
- etl/ (SQL scripts for staging & transforming)
- queries/ (analytical queries)
